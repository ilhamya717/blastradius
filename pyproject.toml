[project]
name = "blastradius"
version = "0.1.0"
description = "Reachability-aware supply-chain risk scanner (Python, JS/TS)"
requires-python = ">=3.9"
license = "MIT"
license-files = ["LICENSE"]
dependencies = [
    "requests>=2.31",
    "packaging>=23.0",
    "rich>=13.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
blastradius = "blastradius.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["blastradius*"]
